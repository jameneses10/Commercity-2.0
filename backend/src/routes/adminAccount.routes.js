const express=require('express'); const authRequired=require('../middlewares/authRequired'); const requireRole=require('../middlewares/requireRole'); const c=require('../controllers/account.controller'); const {resolveDeleteRequestValidator,deleteRequestDetailValidator}=require('../validators/account.validators');
const router=express.Router(); router.use(authRequired,requireRole('administrador')); router.get('/account-delete-requests',c.adminDeleteRequests); router.get('/account-delete-requests/:id',deleteRequestDetailValidator,c.adminDeleteRequestDetail); router.patch('/account-delete-requests/:id',resolveDeleteRequestValidator,c.adminResolveDeleteRequest); module.exports=router;
